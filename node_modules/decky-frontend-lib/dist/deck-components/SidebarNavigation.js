import { findModuleChild } from '../webpack';
export const SidebarNavigation = findModuleChild((mod) => {
    for (let prop in mod) {
        if (mod[prop]?.toString()?.includes('"disableRouteReporting"')) {
            return mod[prop];
        }
    }
    return null;
});
